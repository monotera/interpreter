main miprograma {
	//Programa de prueba de la clase Teoria de la computacion
	
	var y;
	y = 2200;
	
	cout ("Primer Condicional");
	
	if ((y % 4 == 0) && ((y % 100 != 0) || (y % 400 == 0))){
		cout ( "El anio es bisiesto" );
		cout( y );
	} else {
		cout ( "El anio no es bisiesto" );
		cout(y);
	}
	
	cout ("Segundo Condicional");
	
	y = 1998;
	
	if ((y % 4 == 0) && ((y % 100 != 0) || (y % 400 == 0))){
		cout ( "El anio es bisiesto" );
		cout(y);
	} else {
		cout ( "El anio no es bisiesto" );
		cout(y);
	}
	
	cout ("Tercer Condicional");
	
	y = 1994;
	
	if ((y % 4 == 0) && ((y % 100 != 0) || (y % 400 == 0))){
		cout ( "El anio es bisiesto" );
		cout(y);
	} else {
		cout ( "El anio no es bisiesto" );
		cout(y);
	}
	
}